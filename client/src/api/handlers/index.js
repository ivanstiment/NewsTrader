import {
  getErrorMessage,
  getToastType,
  handleApiError,
  handleError
} from "./error.handler";
export {
  getErrorMessage,
  getToastType,
  handleApiError,
  handleError
};

// Exportación por defecto con todos los handlers
export default {
  getErrorMessage,
  getToastType,
  handleApiError,
  handleError
};
